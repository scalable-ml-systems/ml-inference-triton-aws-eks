apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-latency-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  latency-dashboard.json: |
    {{ .Files.Get "dashboards/latency-dashboard.json" | nindent 4 }}
